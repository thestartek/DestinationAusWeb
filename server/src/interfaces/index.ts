export type CreateBlogType = {
  title: string;
  description: string;
  imageUrl: string;
  source?: string;
};

export type CreateArticleType = {
  title: string;
  description: string;
  imageUrl: string;
  source?: string;
};
