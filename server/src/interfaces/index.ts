export type CreateBlogType = {
  title: string;
  description: string;
  imageUrl: string;
  source?: string;
};

export type CreateNewsType = {
  title: string;
  description: string;
  imageUrl: string;
  source: string;
};
