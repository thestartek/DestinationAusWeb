export type BlogType = {
  _id: string;
  title: string;
  description: string;
  imageUrl: string;
  source: string;
};

export type NewsType = {
  _id: string;
  title: string;
  description: string;
  imageUrl: string;
  source: string;
};
